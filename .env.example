# Copy to .env.local and fill in. NEVER commit real values.
# All variables are server-side only - do not add NEXT_PUBLIC_ to any of them.

# AI providers
GROQ_API_KEY=
GEMINI_API_KEY=

# The approved corpus is local and gitignored. Populate it with:
# npm run fetch-corpus
